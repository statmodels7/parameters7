% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inverse_of.R
\name{param_value.InverseParam}
\alias{param_value.InverseParam}
\alias{param_free.InverseParam}
\title{Value and Inverse Map of an Inverse Parameter}
\arguments{
\item{s}{An \code{\link[=InverseParam]{InverseParam()}} object.}

\item{eta}{A numeric vector of length \code{s@n_free}, already checked by the
generic.}

\item{m}{A symmetric positive definite matrix of side \code{s@dimension}.}

\item{...}{Unused, and accepted so the signature matches the generic's.}
}
\value{
\code{param_value()} a \code{s@dimension} square matrix, \code{param_free()} a
numeric vector of length \code{s@n_free}.
}
\description{
\code{param_value()} returns the inner family's inverse, read through
\code{\link[=param_solve]{param_solve()}} so that a family with a closed inverse never reaches a
factorization. \code{param_free()} inverts the matrix given and hands it to the
inner family's own inverse map, so the free vector is the inner one and the
round trip closes on it.
}
\seealso{
\code{\link[=inverse_of]{inverse_of()}} for the family.
}
\keyword{internal}
