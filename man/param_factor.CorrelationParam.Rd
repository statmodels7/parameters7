% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlation_matrix.R
\name{param_factor.CorrelationParam}
\alias{param_factor.CorrelationParam}
\title{Factor of a Correlation Parameter}
\arguments{
\item{s}{A \code{\link[=CorrelationParam]{CorrelationParam()}} object.}

\item{eta}{A numeric vector of free values, of length \code{s@n_free}, already
checked by the generic.}

\item{...}{Unused, and accepted so the signature matches the generic's.}
}
\value{
A \code{s@dimension} by \code{s@dimension} lower triangular numeric matrix with
a positive diagonal and unit row norms, satisfying
\code{L \%*\% t(L) == param_value(s, eta)}, and carrying no dimnames.
}
\description{
Returns \eqn{L} by assembling it from the angles, so no factorization is
taken: the factor is what the parametrization holds. Its rows are unit
vectors, which is where \eqn{R = LL^\top} gets its unit diagonal.

It is the natural way to simulate a correlated standard normal vector:
\eqn{Lz} with \eqn{z} standard normal has correlation matrix \eqn{R} and unit
variances.
}
\seealso{
\code{\link[=param_value.CorrelationParam]{param_value.CorrelationParam()}} for the matrix, and
\code{\link[=param_factor.matrix_parameter]{param_factor.matrix_parameter()}} for what a family without a closed form
pays.
}
\keyword{internal}
