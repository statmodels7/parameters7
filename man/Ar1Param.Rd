% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ar1.R
\name{Ar1Param}
\alias{Ar1Param}
\title{AR(1) Parameter}
\usage{
Ar1Param(
  param_name = character(0),
  n_free = integer(0),
  free_names = character(0),
  param_params = list(),
  dimension = integer(0),
  rank = integer(0),
  null_basis = integer(0)
)
}
\arguments{
\item{param_name}{A single character string naming the family.}

\item{n_free}{The length \eqn{d} of the free vector: a single non-negative
integer, agreeing with \code{length(free_names)}.}

\item{free_names}{A character vector of length \code{n_free}, one label per free
value, in the order the free vector holds them. Must be unique.}

\item{param_params}{A list of whatever the family needs in order to evaluate
itself, read only by that family's own methods.}

\item{dimension}{The side \eqn{p} of the matrix: a single integer, no
smaller than 1. The validator rejects a vector and a value below 1.}

\item{rank}{The rank of the matrix the family produces, a single integer in
\code{0:dimension}. It is a property of the family, so a family whose value is
positive definite at every \eqn{\eta} declares \eqn{p} here.}

\item{null_basis}{A \code{dimension} by \code{dimension - rank} numeric matrix whose
columns are an orthonormal basis of the common null space. Use
\code{\link[=param_null_basis]{param_null_basis()}} to obtain one, or \code{matrix(numeric(0), dimension, 0)}
for a full-rank family. The validator rejects any other shape, and
reports both the rank and the shape when the two disagree.}
}
\value{
An object of class \code{Ar1Param}, a subclass of \code{\link[=matrix_parameter]{matrix_parameter()}}
adding no properties of its own. \code{param_params} holds \code{link_scale} and
\code{link_rho}, the second a \code{linkfunctions7::rhobit_link()} at every dimension.
\code{n_free} is 2 and \code{rank} is \eqn{p}.
}
\description{
The S7 class of first-order autoregressive covariance matrices: equal
variances and a correlation falling geometrically with the lag,
\eqn{M_{ij} = \sigma^2 \rho^{|i-j|}}. Two free values at every dimension, and
the correlation is unrestricted in \eqn{(-1, 1)}, unlike
\code{\link[=compound_symmetry]{compound_symmetry()}}'s.

\code{\link[=ar1]{ar1()}} builds one. It shares its whole derivative skeleton with
\code{\link[=compound_symmetry]{compound_symmetry()}} through \code{\link[=econ_scalars]{econ_scalars()}}, \code{\link[=econ_derivative]{econ_derivative()}} and
\code{\link[=econ_logdet_derivative]{econ_logdet_derivative()}}; what differs is the pattern and the
log-determinant terms.
}
\examples{
# Two free values whatever the dimension, and a rhobit correlation.
s <- ar1(4)
S7::S7_inherits(s, Ar1Param)
c(p4 = ar1(4)@n_free, p20 = ar1(20)@n_free)
s@free_names

# Unlike compound symmetry, the bound does not move with the dimension.
t(vapply(c(2, 4, 20), function(p) ar1(p)@param_params$link_rho@link_bounds,
         numeric(2)))

}
\seealso{
\code{\link[=ar1]{ar1()}}, the constructor, \code{\link[=compound_symmetry]{compound_symmetry()}} for the sibling
family, \code{\link[=autoregressive]{autoregressive()}} for higher orders, and \code{\link[=matrix_parameter]{matrix_parameter()}} for
the properties this inherits.
}
