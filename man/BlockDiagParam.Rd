% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_diag.R
\name{BlockDiagParam}
\alias{BlockDiagParam}
\title{Distinct Blocks of a Matrix Parameter}
\usage{
BlockDiagParam(
  param_name = character(0),
  n_free = integer(0),
  free_names = character(0),
  param_params = list(),
  dimension = integer(0),
  rank = integer(0),
  null_basis = integer(0)
)
}
\arguments{
\item{param_name}{A single character string naming the family.}

\item{n_free}{The length \eqn{d} of the free vector: a single non-negative
integer, agreeing with \code{length(free_names)}.}

\item{free_names}{A character vector of length \code{n_free}, one label per free
value, in the order the free vector holds them. Must be unique.}

\item{param_params}{A list of whatever the family needs in order to evaluate
itself, read only by that family's own methods.}

\item{dimension}{The side \eqn{p} of the matrix: a single integer, no
smaller than 1. The validator rejects a vector and a value below 1.}

\item{rank}{The rank of the matrix the family produces, a single integer in
\code{0:dimension}. It is a property of the family, so a family whose value is
positive definite at every \eqn{\eta} declares \eqn{p} here.}

\item{null_basis}{A \code{dimension} by \code{dimension - rank} numeric matrix whose
columns are an orthonormal basis of the common null space. Use
\code{\link[=param_null_basis]{param_null_basis()}} to obtain one, or \code{matrix(numeric(0), dimension, 0)}
for a full-rank family. The validator rejects any other shape, and
reports both the rank and the shape when the two disagree.}
}
\value{
An object of class \code{BlockDiagParam}, a subclass of
\code{\link[=matrix_parameter]{matrix_parameter()}} adding no properties of its own. \code{param_params} holds
\code{blocks}, \code{labels}, \code{rows} and \code{free} (the ranges each block occupies in the
matrix and in the free vector) and \code{owner} (the block each free value
belongs to). \code{dimension}, \code{n_free} and \code{rank} are the sums of the blocks'.
}
\description{
The S7 class of a block-diagonal matrix built from several matrix parameters,
each block a family of its own and each carrying its own stretch of the free
vector. \code{\link[=block_diag]{block_diag()}} builds one.

It is the composition for independent groups of coefficients whose structures
differ, where \code{\link[=kron_identity]{kron_identity()}} is the one for identical blocks sharing a
single free vector.
}
\examples{
# Everything the composite is, is the sum of what the blocks are.
s <- block_diag(subject = log_cholesky(2), time = ar1(3))
c(dimension = s@dimension, n_free = s@n_free, rank = s@rank)

# And the free names carry the block's label, so two blocks of the same
# family stay distinguishable.
block_diag(a = ar1(3), b = ar1(4))@free_names

}
\seealso{
\code{\link[=block_diag]{block_diag()}}, the constructor, \code{\link[=kron_identity]{kron_identity()}} for identical
blocks, and \code{\link[=matrix_parameter]{matrix_parameter()}} for the properties this inherits.
}
