% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diag_struct.R
\name{DiagStruct}
\alias{DiagStruct}
\title{Diagonal Structure}
\usage{
DiagStruct(
  struct_name = character(0),
  dimension = integer(0),
  n_free = integer(0),
  free_names = character(0),
  rank = integer(0),
  null_basis = integer(0),
  role = character(0),
  struct_params = list()
)
}
\arguments{
\item{struct_name}{A single character string naming the family.}

\item{dimension}{The side \eqn{p} of the matrix.}

\item{n_free}{The length \eqn{d} of the free vector.}

\item{free_names}{A character vector of length \code{n_free}, one label per
free value. Fixed at construction: every consumer builds parameter tables
from these.}

\item{rank}{The rank of the matrix the family produces. Equal to \code{dimension}
for a definite family, and strictly less for a rank-deficient precision,
where it is the dimension of the space the quadratic form penalises.}

\item{null_basis}{A \code{dimension} by \code{dimension - rank} matrix whose columns are
an orthonormal basis of the null space, or a matrix with no columns when
the family is of full rank.}

\item{role}{One of \code{"covariance"}, \code{"precision"} or
\code{"either"}. A label: no method reads it and no result depends on it.
It exists because the name of a family does not say which side of a model
it parametrises, and the two are different models -- the inverse of a
compound-symmetry matrix is compound symmetry, while the inverse of an
AR(1) is tridiagonal and not AR(1).}

\item{struct_params}{A list of whatever the family needs to evaluate itself.}
}
\value{
An object of class \code{DiagStruct}.
}
\description{
The S7 class of diagonal positive matrices, one free value per entry.
Constructed by \code{\link{diag_struct}} or \code{\link{scalar_struct}}.
}
\examples{
S7::S7_inherits(diag_struct(3), DiagStruct)

}
\seealso{
\code{\link{diag_struct}}, \code{\link{scalar_struct}}
}
