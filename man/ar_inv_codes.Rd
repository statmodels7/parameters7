% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ar1_inv.R
\name{ar_inv_codes}
\alias{ar_inv_codes}
\title{The Sub-Multiset Codes of a Derivative Order}
\usage{
ar_inv_codes(idx, n)
}
\arguments{
\item{idx}{The index tuples of the order, as \code{\link[=param_tuple_indices]{param_tuple_indices()}}
returns them.}

\item{n}{The number of free values.}
}
\value{
A list with \code{pow}, the base-five weights; \code{codes}, one integer
vector per tuple holding the code of \code{t[take]} for every subset of
positions in the order the bits enumerate them; \code{comp}, the matching codes
of the complements; and \code{all}, every distinct code with \code{cnt}, the count
vector each stands for.
}
\description{
Enumerates, once per order, every sub-multiset of every index tuple the
order carries, together with the integer code each is stored under. A
sub-multiset is held as its COUNT VECTOR, one entry per free value, and its
code is that vector read as a base-five numeral.
}
\details{
The strings this replaces were 65 per cent of the cost of a fourth-order
component, measured: \code{sort()} and \code{paste()} inside the key, not the
arithmetic. Counts need neither, \code{tabulate()} being one C call, and the code
indexes a list directly. Five is a safe base because a derivative order here
is at most four, so no count can reach it.
}
\seealso{
\code{\link[=ar_inv_derivative]{ar_inv_derivative()}}, the only caller.
}
\keyword{internal}
