% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inverse_of.R
\name{param_solve.InverseParam}
\alias{param_solve.InverseParam}
\alias{param_factor.InverseParam}
\title{Solve and Factor of an Inverse Parameter}
\arguments{
\item{s}{An \code{\link[=InverseParam]{InverseParam()}} object.}

\item{eta}{A numeric vector of length \code{s@n_free}, already checked by the
generic.}

\item{b}{A numeric matrix or vector with \code{s@dimension} rows, or \code{NULL} for
the whole inverse.}

\item{...}{Unused, and accepted so the signature matches the generic's.}
}
\value{
\code{param_solve()} the inner value, or its product with \code{b};
\code{param_factor()} a lower triangular matrix \code{L} with \code{tcrossprod(L)} the
value.
}
\description{
\code{param_solve()} is the inner family's \strong{value}, no inversion being needed:
the inverse of \eqn{S^{-1}} is \eqn{S}. \code{param_factor()} is the lower
triangular \eqn{L} with \eqn{LL^\top} the value, which is the package's
convention and the transpose of what \code{\link[base:chol]{base::chol()}} returns.
}
\seealso{
\code{\link[=inverse_of]{inverse_of()}} for the family.
}
\keyword{internal}
