% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kron_identity.R
\name{kron_accessors}
\alias{kron_accessors}
\title{The Inner Parameter, the Block Count, and the Lift}
\arguments{
\item{s}{A \code{\link[=KronIdentityParam]{KronIdentityParam()}} object.}

\item{m}{For \code{.kron_lift()}, the number of blocks.}

\item{x}{A numeric matrix to replicate.}
}
\value{
\code{.kron_inner()} a \code{\link[=matrix_parameter]{matrix_parameter()}}, \code{.kron_m()} a single integer,
\code{.kron_lift()} an \code{m * nrow(x)} by \code{m * ncol(x)} numeric matrix with \code{x} on
the diagonal blocks and zeros elsewhere.
}
\description{
The three one-line accessors every \code{\link[=KronIdentityParam]{KronIdentityParam()}} method uses.
\code{.kron_inner()} returns the per-block parameter, \code{.kron_m()} the number of
blocks, and \code{.kron_lift()} places a \code{d} by \code{d} matrix into \code{m} identical
diagonal blocks as \code{kronecker(diag(m), x)}.
}
\details{
They exist so that the twelve methods below read as one line each and the
storage of \code{param_params} appears in one place. \code{.kron_lift()} is the whole
arithmetic of the composition: the value, every derivative component and the
factor are the inner quantity passed through it.

Note that \code{kronecker()} drops dimnames, so a lifted matrix carries none where
the inner one carried \code{v1}, \code{v2}, ...
}
\seealso{
\code{\link[=kron_identity]{kron_identity()}}, which stores what these read.
}
\keyword{internal}
