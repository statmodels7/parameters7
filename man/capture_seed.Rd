% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_parameter.R
\name{capture_seed}
\alias{capture_seed}
\alias{restore_seed}
\title{Capture and Restore the Caller's Random Stream}
\usage{
capture_seed()

restore_seed(old)
}
\arguments{
\item{old}{The value \code{\link[=capture_seed]{capture_seed()}} returned, or \code{NULL}.}
}
\value{
\code{capture_seed()} returns the saved \code{.Random.seed}, an integer vector,
or \code{NULL}. \code{restore_seed()} returns \code{NULL} invisibly and is called for its
effect on the global environment.
}
\description{
\code{capture_seed()} reads \code{.Random.seed} from the global environment, returning
\code{NULL} when there is none, and \code{restore_seed()} puts back what it was given.
Together they let a function draw from a fixed seed, so that its report is
the same on two runs, without leaving the caller's stream changed.
}
\details{
A validator wants both properties and they pull against each other. Drawing
from the caller's stream makes the worst error reported move between runs;
calling \code{set.seed()} fixes that and replaces whatever state the caller had,
so a call inside a simulation silently changes the simulation. Saving the
state on entry and restoring it with \code{\link[base:on.exit]{base::on.exit()}} gives the fixed draw
and leaves the caller alone.

\code{restore_seed(NULL)} removes \code{.Random.seed} again, which is the right answer
when the caller had never drawn a random number: leaving the seed the
validator set behind would be the leak this exists to prevent.
}
\examples{
# the property the pair exists for, through the public interface: a
# validator call leaves the caller's stream exactly as it found it
set.seed(42)
want <- rnorm(1)
set.seed(42)
invisible(check_parameter(log_cholesky(2), verbose = FALSE))
identical(rnorm(1), want)

}
\keyword{internal}
