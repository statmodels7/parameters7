% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matrix_log.R
\name{MatrixLogParam}
\alias{MatrixLogParam}
\title{Matrix Logarithm Parameter}
\usage{
MatrixLogParam(
  param_name = character(0),
  n_free = integer(0),
  free_names = character(0),
  param_params = list(),
  dimension = integer(0),
  rank = integer(0),
  null_basis = integer(0)
)
}
\arguments{
\item{param_name}{A single character string naming the family.}

\item{n_free}{The length \eqn{d} of the free vector: a single non-negative
integer, agreeing with \code{length(free_names)}.}

\item{free_names}{A character vector of length \code{n_free}, one label per free
value, in the order the free vector holds them. Must be unique.}

\item{param_params}{A list of whatever the family needs in order to evaluate
itself, read only by that family's own methods.}

\item{dimension}{The side \eqn{p} of the matrix: a single integer, no
smaller than 1. The validator rejects a vector and a value below 1.}

\item{rank}{The rank of the matrix the family produces, a single integer in
\code{0:dimension}. It is a property of the family, so a family whose value is
positive definite at every \eqn{\eta} declares \eqn{p} here.}

\item{null_basis}{A \code{dimension} by \code{dimension - rank} numeric matrix whose
columns are an orthonormal basis of the common null space. Use
\code{\link[=param_null_basis]{param_null_basis()}} to obtain one, or \code{matrix(numeric(0), dimension, 0)}
for a full-rank family. The validator rejects any other shape, and
reports both the rank and the shape when the two disagree.}
}
\value{
An object of class \code{MatrixLogParam}, a subclass of
\code{\link[=matrix_parameter]{matrix_parameter()}} adding no properties of its own. \code{param_params} holds
\code{positions}, the row and column of each free value. \code{n_free} is
\eqn{p(p+1)/2} and \code{rank} is \eqn{p}.
}
\description{
The S7 class of unstructured symmetric positive definite matrices in the
matrix logarithm parametrization, \eqn{M = \exp(S)} with \eqn{S} symmetric and
its lower triangle read straight off the free vector. It is the other chart
onto the same cone \code{\link[=log_cholesky]{log_cholesky()}} parametrizes, with a different set of
quantities coming free.

\code{\link[=matrix_log]{matrix_log()}} builds one. Every quantity is exact, and every derivative goes
through the eigendecomposition of \eqn{S}, so the object holds no auxiliary
data at all.
}
\examples{
# The same cone as log_cholesky(), the same number of free values.
s <- matrix_log(3)
c(matrix_log = s@n_free, log_cholesky = log_cholesky(3)@n_free)

# But different free names: no entry is transformed here.
rbind(matrix_log = s@free_names, log_cholesky = log_cholesky(3)@free_names)

}
\seealso{
\code{\link[=matrix_log]{matrix_log()}}, the constructor, \code{\link[=log_cholesky]{log_cholesky()}} for the other
unstructured chart, and \code{\link[=matrix_parameter]{matrix_parameter()}} for the properties this
inherits.
}
