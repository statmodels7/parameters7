% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kron_identity.R
\name{param_solve.KronIdentityParam}
\alias{param_solve.KronIdentityParam}
\title{Solve of a Block Replication}
\arguments{
\item{s}{A \code{\link[=KronIdentityParam]{KronIdentityParam()}} object.}

\item{eta}{A numeric vector of free values, of length \code{s@n_free}, already
checked by the generic.}

\item{b}{A numeric matrix with \code{s@dimension} rows, already coerced from a
vector and defaulted to the identity by the generic.}

\item{...}{Unused, and accepted so the signature matches the generic's.}
}
\value{
A numeric matrix with \code{s@dimension} rows and as many columns as \code{b}.
}
\description{
Solves blockwise: \eqn{M^{-1} = I_m \otimes S^{-1}}, so the rows of \code{b}
belonging to each block are handed to the inner parameter's own
\code{\link[=param_solve]{param_solve()}} in turn. Nothing of side \eqn{md} is factorized, and the
method inherits whatever the inner parameter does, including a closed inverse
where it has one.

Measured at \eqn{m = 3} over a 2 x 2 log-Cholesky covariance, the result
agrees with \code{base::solve()} on the assembled matrix to
\eqn{3 \times 10^{-17}}.

The generic has already rejected a rank-deficient composite, which a deficient
inner parameter produces, and filled \code{b} with the identity when the caller
left it out.
}
\seealso{
\code{\link[=param_factor.KronIdentityParam]{param_factor.KronIdentityParam()}} for the lifted factor, and
\code{\link[=kron_identity]{kron_identity()}} for why the deficiency is inherited.
}
\keyword{internal}
