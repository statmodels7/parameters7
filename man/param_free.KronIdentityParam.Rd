% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kron_identity.R
\name{param_free.KronIdentityParam}
\alias{param_free.KronIdentityParam}
\title{Free Vector of a Block Replication}
\arguments{
\item{s}{A \code{\link[=KronIdentityParam]{KronIdentityParam()}} object.}

\item{m}{A symmetric numeric matrix of side \code{s@dimension}, block diagonal
with \code{s@param_params$m} identical blocks, already checked for shape and
symmetry by the generic.}

\item{...}{Unused, and accepted so the signature matches the generic's.}
}
\value{
A numeric vector of length \code{s@n_free}, named by \code{s@free_names}.
}
\description{
Reads the free vector from the \strong{first} diagonal block, by inverting the
inner parameter there, and then checks the whole matrix against the
replication that free vector implies. A matrix whose blocks are not identical,
or whose first block is outside the inner family, is rejected.

The rejection is complete because the whole rebuild is compared, not the
blocks pairwise: the first block might invert cleanly while the fourth is
something else entirely.
}
\seealso{
\code{\link[=param_value.KronIdentityParam]{param_value.KronIdentityParam()}}, the map this inverts.
}
\keyword{internal}
