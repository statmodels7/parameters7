% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoregressive.R
\name{ar_pack_col}
\alias{ar_pack_col}
\title{The Column of a Packed Derivative Record}
\usage{
ar_pack_col(n, order = 0L, tuple = NULL)
}
\arguments{
\item{n}{The number of free values, \eqn{q + 1}.}

\item{order}{The derivative order 1 to 4, or 0 for the value.}

\item{tuple}{The index tuple, 1-based, of length \code{order}. Ignored at order 0.}
}
\value{
A single column index.
}
\description{
Locates a derivative component in a row of \code{\link[=ar_taylor]{ar_taylor()}}'s output. The value
sits in column 1 and the full tensors of orders one to four follow it in
row-major order, so order \eqn{k} begins at column
\eqn{1 + \sum_{j<k} n^{j}} and the tuple \eqn{(t_1, \dots, t_k)} sits
\eqn{\sum_i (t_i - 1) n^{k-i}} further on.
}
\details{
At \eqn{n = 3} the value is column 1, the three first-order components are
columns 2 to 4, and the nine second-order ones are columns 5 to 13:
\eqn{(1,1)} is 5, \eqn{(1,2)} is 6 and \eqn{(3,3)} is 13. The tensor is stored
\strong{full}, with one column per ordered tuple, so \eqn{(1,2)} and \eqn{(2,1)} are two
columns holding the same number; the caller reads whichever one
\code{\link[=param_tuple_indices]{param_tuple_indices()}} hands it.
}
\seealso{
\code{\link[=ar_taylor]{ar_taylor()}} for the layout this describes.
}
\keyword{internal}
